{
  "name": "senior-monitor-server",
  "version": "1.0.0",
  "description": "Backend for the Senior Monitor system – receives data from ESP8266 and streams it to the PWA via WebSocket.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "ws": "^8.16.0"
  }
}
